// This is here to prevent builds failing as the project doesn't have tests.
// Remove this once tests have been added.
describe("Placeholder test", () => {
  test("placeholder", () => {
    expect(true).toBe(true)
  })
})